version: "2"

options:
  verbose: true
  compose-dev-file-path: 
    - docker-compose.sync.yml
    - docker-compose.dev.yml
  
syncs:
  esportsvideos-php-sync:
    src: './'
    sync_userid: '82'
    sync_excludes: ['.idea/', '.git/', 'node_modules/']
    sync_excludes_type: 'Name'
    watch_excludes: ['.*/.git', '.*/node_modules', '.*/idea', '.gitignore']
